# How to start a jupyter notebook (in a conda python3 environment):

* install: `pip install --upgrade pip`
* `pip install jupyter`
* If necessary `source envvars.sh`
* `jupyter notebook`
(http://jupyter.org/install)
